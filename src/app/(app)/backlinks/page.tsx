import { BacklinksClient } from '@/components/backlinks/backlinks-client';

export const metadata = {
    title: 'Organic Backlink Network | Blog OS',
    description: 'Join our exclusive organic backlink network to grow your SEO authority.',
};

export default function BacklinksPage() {
    return <BacklinksClient />;
}
